id: t08
tier: large
repo:
  url: local:.
  commit: pinned-by-content-in-git
  localPath: .
prompt: 'Find where audit events are validated and list callers.'
success:
  - kind: answer-contains
    needle: 'AuditEventSchema'
  - kind: answer-contains
    needle: 'audit-file'
fixtureLogs:
  vanilla: fixtures/sessions/long-agentic.jsonl
  redutok: fixtures/sessions/small.jsonl
